name: eds
version: "1.0.0"
config-version: 2

profile: eds

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

models:
  eds:
    staging:
      +schema: staging
    intermediate:
      +schema: intermediate
    marts:
      +schema: marts